# 전수 버그 리뷰·수정 감사 (2026-07-10)

멀티에이전트 리뷰 3회(수정트랙 리뷰 40 + 파이프라인 심층 62 + 수정분 감사 35 에이전트,
발견별 2-렌즈 적대 검증) 결과. 모든 발견은 confirmed = 검증자 만장일치.

## A. 수정 완료 (이 커밋)

### 1차 수정 (수정트랙 리뷰 발견분)
| # | 심각도 | 버그 | 수정 |
|---|---|---|---|
| 1 | critical | `MS::d_rx[4][…]` OOB write — 8-port/MS_M>4 런에서 인접 멤버(UE 방향각 alpha/beta/gamma) 오염. layout-sensitivity의 유력 근원 | 차원을 `MAX_MS_*`로 확장 (h/MS.h) |
| 2 | major | in-flight TB의 layer 수 미동결 — rank 변화 시 조기 ACK(미결 layer 폐기)/유령 layer/첫-RB만 캡처/accumulator wipe | `num_layers_actual` 첫 전송 시 슬롯 max로 동결 + `layers_rx_this_slot` 분리(미전송 pending layer 코인/IR 스킵) + 전 RB 캡처 + grow-preserve + per-layer 기아 드롭 |
| 3 | major | 프리코더 열-스트림 비정렬 — 예약 R=self_RI vs 적재 min(PMI열,RI) → 지연 PMI보다 RI 커진 UE 이후 전 열 밀림 | 예약 루프도 PMI 열수로 캡 (예약==적재) |
| 4 | major | esinr_fb가 placeholder capacity=1.0(=0dB)을 추정 기준으로 소비 (USE_PRECODING_BASED_SINR=0 경로) — "편향 보정기"가 "절대 추적기"로 오동작 | `Set_AVR_Cqi`가 CQI-기반 linear 추정치 기록. NCJT 경로 미초기화 avr_capacity + `cqi_selected=capacity` 복붙 버그 동시 수정 (NCJT 브랜치는 comp_mode_flag 항상 0으로 dead — 방어적 수정) |
| 5 | major | 11ee679 게이트 완화 회귀 — Calibration+오버라이드 조합 미가드 | Calibration 거부 추가 |
| 6 | minor | SU-fallback 편향 (SU만 ×R) | rank 가중 (후속 감사로 재보정 — 아래) |
| 7 | minor | .dat 누락 시 getchar 후 전-실패 진행 | FATAL + exit(1) |
| 8 | minor | `Assign_Row_Beams`가 정렬된 static_gain에 flat 인덱스로 기록 | `.second==flat` 검색 |

### 감사 보완 (수정 8건에 대한 적대 감사 발견분)
| # | 보완 |
|---|---|
| B1-1 | **cfg 주석 토큰 하이잭**: `# BS_Tx_Power 46`/`# UT_Noise_Figure 7`이 5개 cfg에서 실파싱되고 있었음(DU_B + rowbeam 3종 + fixcheck). 주석 재작성 + 117개 파라미터 전수 재스캔 0건. ⚠️ row-beam 3-arm A/B는 세 arm 동일 조건이라 상대 비교 유효, 절대값은 46dBm/NF7 하에서 측정된 것 |
| B1-2 | Calibration 가드 `==1`→`!=0` (2사이트; 비1 양수 우회 차단) |
| B1-3 | SU-fallback MU 합을 실예약 layer 수로 캡(PMI+mx 예산), SU 후보도 PMI 캡 |
| B1-4 | 지향성 UE 서브어레이 >8×8 방어(파스+런타임 2중 — `ue_pos[8][8]`/`ue_w·ue_v·ue_virtualization_weight_wv[…][8]` 잠재 오버플로), handheld_num_ports [1,8] 검증 |
| B1-5 | stall cap을 per-layer 연속 기아 카운터로 (TB-레벨 슬롯 캡이 간헐 서빙 layer의 최종 결합 시도를 선점하던 문제) |

검증: 수정 후 동일 cfg 2회 bit-identical(텍스트+CDF), MATLAB 스택/row-beam 스모크 정상.

## B. 미수정 확정 발견 (파이프라인 심층 리뷰, 23건) — 반영 대기

### Batch 2 — A/B 비교에도 영향 (우선 수정 권장)
- **PF stream-0 크레딧** (scheduling.cpp:706/:729/:1103, critical): 평균레이트 갱신이 stream 0만 검사 →
  co-scheduled MU UE는 영구 무료 서비스 → PF 공정성 근본 왜곡
- **AVR_Cqi_update 이중/감산 갱신** (:2741-2776): CQI_AVR 음수화 + truthiness 가드가 기아 UE 리셋
- **비에르미트 고유해** (channel_update.cpp:1366): `(B+C)^{-1}A`에 SelfAdjointEigenSolver — CQI 수신방향 무효
- **per_layer_mcs가 USE_PRECODING_BASED_SINR=0에서 침묵 비활성** (레이어별 MCS 미분화 — 과거 per-layer
  실험은 per-layer BLER/HARQ만 검증한 것. 옵션: 경고/가드 또는 feedback 경로에 per-layer 추정 추가)
- 통계 정규화 (PerdropStatistics:587-598, 단일셀 진단 7×/21× 디플레이트), initial_bler 창 혼합,
  received_sinr_avg 왜곡, chase 버퍼 num_rb 캡

### Batch 3 — 절대치 변경 (수정 시 전 기존 수치 재베이스라인 필요)
- **TX 총전력 제약 부재** (critical): 스트림마다 full bs_maxpower → K-스트림 셀이 K배 방사 (간섭 셀 1배 모델)
- **TBS overhead 이중 적용**: 156 RE 캡 위에 (1−0.33) 추가 → 전 TBS/SE ~33% 과소 (TS 38.214 위반)
- **처리량 창/분모 불일치**: 누적 t>N_pf(~404슬롯) vs 분모 run_times−5(495) → 절대 SE ~22% 추가 과소
- TBS 양자화 스펙 편차 (3834≠3824 등)

### Batch 4 — 켤레 쌍 (플립 실험으로 판정)
- CQI가 conj(w)로 평가(channel_update.cpp:1345) + FDD RZF가 conj(v) 송신(scheduling.cpp Lambda
  .transpose()) — 상호 일관되나 **시스템 전체가 거울상 빔으로 동작 중일 가능성**. 128포트 복소 빔에서
  conj(v)는 채널 행공간과 거의 직교 → 두 라인 동시 수정 시 SE 대폭 상승 예상. 동일시드 플립 A/B로 판정

### 기타 (minor, 물리 정합)
- USE_RAY_LEVEL_DOPPLER 플래그가 라이브 경로에서 무시(클러스터-중심 Doppler만; 3km/h 영향 미미,
  고속/고주파 연구 전 수정 필요), UE 이동방향 elevation 랜덤화(수평이어야 함), per-layer TBS의 RB 수 과대,
  MMSE 필터 공분산 열잡음 누락, SU-fallback mx 잔여 이슈 등 — 상세는 워크플로 결과 JSON 참조

## 방법론 메모
- 리뷰: 차원별 finder → 발견마다 도달가능성/스펙 2-렌즈 적대 검증(반박 기조), 만장일치만 confirmed
- 기각 사례가 증명하듯 첫 리뷰의 ~20%는 검증에서 탈락 — 검증 단계 필수
- 수정 자체에 대한 감사에서 6건 보완 발견 — "수정도 리뷰 대상"

## C. 반영 완료 (2026-07-11, Batch 2·3·4 — 커밋 a0a241d + 5f61944)

§B의 대기 항목 전체 반영. 핵심 결과:

### 켤레 쌍 플립 실험 — "거울상 빔" 가설 확정
동일 cfg/시드(1drop×200slot) 플립 전/후:
| 경로 | pre | post | Δ |
|---|---:|---:|---:|
| FDD MATLAB 스택 (FC_A) | 6.587 | **22.966** | **+249%** |
| TDD row-beam (FC_RB) | 19.204 | 21.918 | +14% |

FDD 폭등·TDD 소폭의 예측 패턴 일치 → **FDD eType II 경로는 그동안 conj(v)
거울상 빔을 송신**(Lambda .transpose() + CQI .conjugate()가 상호 일관되게
가려줌). 두 라인 수정으로 128포트 빔포밍 이득 회복. per-layer 1st-tx BLER
0.096 ≈ OLLA 타깃 0.1 — 링크어댑테이션 체인 정상화.

### 반영 요약
- Batch 2: PF 전-스트림 크레딧, AVR_Cqi_update 재설계(미스케줄만+플로어),
  닫힌형 CQI 결합기, per_layer×feedback-SINR 경고 + 정식 cfg
  USE_PRECODING_BASED_SINR 1 전환, 통계 4건 (initial_bler_layer 신설 포함)
- Batch 3: TX 전력 P/K 분배(수신·스케줄러·UE-CQI 일관), TBS 38.214 정확화
  (overhead 이중적용 제거 + Table 5.1.3.2-1 + 경계/정수나눗셈), 처리량 창 정합,
  per-layer TBS RB 수, Ryy 열잡음, UE 수평 이동, ray-Doppler 한계 경고
- Batch 4: 켤레 쌍 (위)

### 절대 수치 단절 (재베이스라인)
Batch 3·4는 절대 SE 스케일을 의도적으로 이동시킴 (TBS +50%, 창 +24%,
전력 −10.8dB@12스트림, 빔 +수 dB). **이 커밋 이전의 모든 절대 수치와
비교 불가** — A/B 상대 결론은 각 시점의 페어드 설계라 방향성 유지.
새 기준: EMIMO_SLS_UMi_MatlabStack.cfg 3-drop 런 (UMi_MatlabStack/).

### 미반영 잔여 (의도적)
- ray-level Doppler 실구현 (per-ray 저장 필요 — 고속/고주파 연구 전 별도 과제)
- dead 경로(Receive_DL/Compute_Num_Bin_Symbol/Update_v2)의 스펙 편차 — 미호출
- NCJT(Type 2/3) 경로의 잔여 이슈 — 현행 실험 범위 밖

## D. 더블체크 + 채널생성 리뷰 (2026-07-11, 커밋 2a8bad2)

Batch 2·3·4에 대한 재감사(55 에이전트) — **수정 자체의 결함 5건** 발견·보완:
1. AVR 감산+플로어 설계 결함 (major): 서빙비율<1/2인 모든 (UE,RB)가 플로어에
   흡수 → PF가 max-C/I로 퇴화. **감산 완전 제거** (EWMA 감쇠만으로 PF 완전)
2. 신규 layer 카운터·per_layer HARQ 상태·pmi_* 누산기 per-drop 리셋 누락
3. Ryy 잡음 수정이 수신기만 (주석의 일관성 주장 거짓) → 스케줄러 2사이트 보완
4. per-UE CSV 분모 미갱신 (스윕 파서 대상 파일)
5. Total_Min 창 불일치

채널 생성(드롭~계수) 심층 리뷰 — 신규 4건 수정:
- **LSP 공간 상관 미배선** (major): 상관 그리드 생성 후 미사용, UE마다 iid —
  마지널만 정상이라 캘리브레이션은 통과했었음. 최근접 격자 샘플링 배선 +
  grid_interval 2000→10 m (17 cfg) + 경고
- **AOD/AOA·ZOD/ZOA 부호 공유** (major, 38.901 Step 7 위반): 독립 X_n으로
- ray 커플링 순열 3중 독립화 (Step 8) + rand_coupling 누수 수정
- **CQI/RI 빔포밍 이득 이중계상** (major): str_signal×H_m → P−PL 규약 통일

부수 확인: 서브클러스터 분할(Table 7.5-5)은 구현돼 있음(Set_SUBCLUSTER).
검증: bit-identical 유지, per-layer 1st-tx BLER 0.101 ≈ OLLA 타깃 —
링크어댑테이션 체인 전 구간 정합. 절대 수치 재이동 → 재베이스라인 재실행.
